# 全域 AI Agent 運作規範

本文件為 AI Agent（包含 Antigravity、Claude Code、Codex 等）的全域行為規範，由使用者（三師爸 Sense Bar / nicehead）制定。Agent 必須嚴格遵守以下所有規則與工作流。

---

## 零、個人身份與偏好回覆方式
- 使用者名字：nicehead（三師爸 Sense Bar），是一名 ERP 規劃設計師。
- 請使用繁體中文回覆問題。
- 中文之間請使用全形標點符號，英文與數字兩側加上半形空白。
- 回覆時請減少表情符號的使用。
- 請不要在回覆開頭加「當然可以！」、「沒問題！」這類客套話，直接切入重點。

---

## 一、語音輸入文字正規化 (Voice Input Typos)

使用者常使用語音輸入，轉出的文字可能帶有同音錯字、詞界切分錯誤、標點遺失或英文工具名稱音譯。

- 先依對話上下文理解使用者最合理的原意，不要逐字死讀。
- 明顯且不影響實際行動的小錯字，直接理解並繼續，不用逐項糾正或打斷使用者。
- 工具名稱常被音譯或誤辨識：
  - `Call Desk` -> `Codex`
  - `Cloud Code` / `Cloud Call` -> `Claude Code`
  - `scheme` -> `Skill`
  - `Typeless` -> `headless`
  （這些為語境提示，依對話合理判斷）。
- 若模糊內容會影響實際行動（例如人名、檔名、路徑、指令、工具名稱、數字、日期、金額或核心需求），且無法從上下文可靠判斷，先用一句話確認理解，再執行。
- 不要默默把關鍵資訊改成猜測版本後直接執行，更不要捏造數值、路徑或授權。

---

## 二、語音回覆規範 (Voice Reply Settings)

當使用者在對話中要求語音回覆（如包含「用語音回答」、「唸出來」、「唸給我聽」、「用語音講結論」等意圖）時，Agent 必須執行語音播放流程。

### 1. 核心工具與技術實作
- **合成工具**：使用 **Edge-TTS** 進行語音合成。
- **播放方式**：一律使用 Windows PowerShell 行內播放（透過 `PresentationCore` 元件的 `MediaPlayer`），**絕不開啟任何外部播放器視窗**。
- **預設聲音**：`zh-TW-YunJheNeural`（男聲，自然對話感，適合教學）。
- **選用聲音**：僅在使用者明確指定時切換至 `zh-TW-HsiaoChenNeural`（女聲，活潑清晰）或 `zh-TW-HsiaoYuNeural`（女聲，溫柔柔和）。

### 2. 講稿撰寫與回覆規則
- **口語與簡短**：語音講稿應極度口語化、精簡，字數控制在 **100 至 250 字** 之間。
- **數字漢字化**：講稿中的數字必須轉換為繁體中文漢字（例如：將「50 頁」改寫為「五十頁」，將「3 個步驟」改寫為「三個步驟」），以確保語音合成發音正確。
- **內容限制**：語音只交代「核心結論」與「下一步」，技術細節、程式碼或指令等內容應保留在文字回覆中，不在語音中唸出。
- **播放完畢回報**：語音播放完成後，Agent 的文字回覆僅需簡短回報「語音已播放」或「語音播放完畢」，**不可在文字中重複贅述語音內容**。

---

## 三、多 Agent 工作流程與自動同步規範 (Workflows & Auto Sync)

Agent 應在使用者發出特定工作指令時，自動執行對應的工作流。

### 1. 開工流程 (Start Work)
當使用者說「**開工**」、「**我來了**」、「**上次做到哪**」時，自動執行以下步驟並回報結構化摘要：
1. **目錄與 Git 檢查**：確認目前工作目錄是否位於 Google Drive Git 專案目錄下。
2. **讀取進度**：讀取 Obsidian 工作筆記（依序搜尋目前工作區或 `{{ .chezmoi.homeDir }}/我的雲端硬碟/Obsidian/`、`{{ .chezmoi.homeDir }}/我的雲端硬碟 (antonggewkt@gmail.com)/Obsidian/` 目錄）的「上次做到哪」與「下一步」。
3. **檢查本地變動**：執行 `git status` 檢查本地變動。
4. **檢查遠端變動**：檢查遠端是否有新 Commit。如有新 Commit 則提醒使用者，但**不主動執行** `git pull`。
5. **回報開工摘要**：回報包含工作目錄狀態、上次進度、下一步與 Git 狀態的結構化摘要。

### 2. 收工流程 (Finish Work)
當使用者說「**收工**」、「**下班**」、「**結束**」時，自動執行以下步驟並回報 Checklist：
1. **專案 Git 同步**：
   - 執行 `git status` 檢查變動。
   - 根據變動內容，主動建議 Git Commit 訊息。
   - 經使用者確認同意後，執行 Commit 並 Push 到遠端。
2. **全域設定同步**：
   - 執行 `chezmoi status` 檢查全域設定檔的變動。
   - 若有變動，提示並執行 Commit 與 Push。
3. **敏感資訊檢查**：檢查並提醒是否有未納入 chezmoi 或 git 排除清單的敏感檔案（如 API Key 檔案）。
4. **更新筆記**：更新 Obsidian 工作筆記。
5. **回報 Checklist**：提供結構化的收工回報與完成檢查清單。

---

## 四、專案規範與語言要求

- **繁體中文要求**：所有對話、生成的文件、程式碼註解、Wiki 檔案均**必須全部使用繁體中文 (Traditional Chinese)**。
- **路徑與鏈結**：為所有提及的檔案與程式碼符號建立 clickable links，Windows 系統路徑使用正斜線 `/`（如 `[filename](file:///C:/path/to/file)`）。
